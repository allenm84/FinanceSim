<Window x:Class="FinanceSim.PopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinanceSim"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        SizeToContent="WidthAndHeight"
        ResizeMode="CanMinimize"
        MinWidth="200"
        WindowStyle="ToolWindow"
        Title="PopupWindow" Height="Auto" Width="Auto">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl Content="{Binding}"/>
        <Grid Grid.Row="1" x:Name="pnlOkCancel">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="80"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnOK" Grid.Column="1" Margin="3" Content="OK" Click="btnOK_Click"/>
            <Button x:Name="btnCancel" Grid.Column="2" Margin="3" Content="Cancel" Click="btnCancel_Click"/>
        </Grid>
    </Grid>
</Window>
